# Screenshots

The images in this folder (`labs.svg`, `lab.svg`, `completion.svg`) are UI previews
that reflect the real theme and layout. To use your own captures instead, drop PNGs
here (e.g. `labs.png`) and update the paths in the main `README.md`.
